# The audit-ledger is a SEPARATE git repo (CLAUDE.md §5). It must never be
# committed into the app repo - doing so would break the tamper-evidence
# claim, since its history would then be rewritable from here.
audit-ledger/

# Generated demo page (rebuild with tools/build_demo.py)
witness-demo.html

# Local Databricks mirror
*.mirror.db
.mirror.db

node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store
shot.js
shot2.js
check.js
check2.js
shot2.js
